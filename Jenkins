pipeline{
    agent{
        label 'maven'
    }
    stages{
        stage 'build'{
            tools{
                maven 'MAVEN_HOME'
                jdk 'JAVA_HOME'
            }
            steps{
                sh 'mvn validate'
                sh 'mvn install'
            }
        }
    }
}
